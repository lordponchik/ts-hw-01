console.log('task-02');

const total: number = 100;
let ordered: number = 50;

console.log(`total = ${total}; ordered = ${ordered}`);
console.log(
  ordered > total ? 'На складе недостаточно твоаров!' : 'Заказ оформлен, с вами свяжется менеджер'
);
console.log('\n');

ordered = 20;

console.log(`total = ${total}; ordered = ${ordered}`);
console.log(
  ordered > total ? 'На складе недостаточно твоаров!' : 'Заказ оформлен, с вами свяжется менеджер'
);
console.log('\n');

ordered = 80;

console.log(`total = ${total}; ordered = ${ordered}`);
console.log(
  ordered > total ? 'На складе недостаточно твоаров!' : 'Заказ оформлен, с вами свяжется менеджер'
);
console.log('\n');

ordered = 130;

console.log(`total = ${total}; ordered = ${ordered}`);
console.log(
  ordered > total ? 'На складе недостаточно твоаров!' : 'Заказ оформлен, с вами свяжется менеджер'
);

console.log('\n');
